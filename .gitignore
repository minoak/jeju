# 비밀키 — 절대 커밋 금지
.env
config.py
# 로컬 프론트 프로토타입 (API 키 하드코딩됨 — 절대 커밋 금지)
app/*.html

# 데이터 — raw/processed/golden/mock 전체 커밋 (팀원 EDA 공유용)
# data 폴더 안 json 은 모두 추적, data 밖에서 우발적으로 생기는 json 만 무시
*.json
!data/**/*.json

# 파이썬
__pycache__/
*.pyc
.ipynb_checkpoints/
venv/

# 벡터DB (빌드 시 생성)
chroma_db/
chroma_smoke/
chroma_seed_test/

# 백업·임시 파일
*.bak
*.bak2
*.tmp
_rebuild_log.txt
_compare_out.txt
_compare_seed_out.txt

# 로컬 카카오 키 주입 (각자 .env 로 생성, 절대 커밋 금지)
web/config.local.js
