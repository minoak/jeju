# 비밀키 — 절대 커밋 금지
.env
config.py
# 로컬 프론트 프로토타입 (API 키 하드코딩됨 — 절대 커밋 금지)
app/*.html

# 데이터 (golden·mock 제외 — 수작업/뼈대 자산은 커밋)
data/raw/
data/processed/
*.json
!data/golden/*.json
!data/mock/*.json

# 파이썬
__pycache__/
*.pyc
.ipynb_checkpoints/
venv/

# 벡터DB (빌드 시 생성)
chroma_db/

# 로컬 카카오 키 주입 (각자 .env 로 생성, 절대 커밋 금지)
web/config.local.js
