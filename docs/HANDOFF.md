# HANDOFF — 작업 인수인계 (다음 세션/팀원용)

> 마지막 작업: 7/9 밤. 작성: 민옥 + SilenceBreaker.
> 이 문서 하나로 "지금 어디까지 왔고 다음이 뭔지"를 잡는 것이 목적.

## 7/9 밤 — 백엔드 대개편: 카드 정본 병합 + 런타임 LLM 철거

**한 줄**: 중복(프릳츠 9조각)은 LLM이 아니라 결정적 병합으로 죽였고, 런타임 생성 LLM은 전부
은퇴 — 정체성은 "근거 있는 검색엔진"으로 정돈 (신뢰가 컨셉). 응답 6~9초 → 1~3초.

- **merge.py 본 구현**: place_id 142그룹 자동 병합 + 이름변형 26건 민옥 검수(전부 O) 반영
  → 이름 1,064개 → **정본 카드 831장** (`cards.json` 층 분리: 정체/요약/근거/신호 + `정본매핑.json`)
  - 블로거 = **union** (max 아님): 해일리 222명(7조각), 프릳츠 211명(9조각)
  - **부속시설 오매칭 방어**: 주차장·충전소 매칭 = place_id 무효(새빌 3조각이 시설 2개에 갈라진 실측),
    펜션·호텔 매칭 = kakao_name만 불채택. 알맹이 완전 일치만 재병합 ('테' 가짜 병합 방지 유지)
  - **폐업 강등 12장**: closed_hint 11곳 + 애월빵공장(민옥 제보, 카카오 MISS 교차 확인).
    아베베비킬라·트라인커피도 블로그 폐업 신호 있었음 (민옥 확인 권장)
- **embed.py 개편**: 정본 단위 재임베딩 → 컬렉션 `cards` 985문서 (blog 644 재임베딩 +
  hybrid 341 벡터 재사용, 폐업·비유지 hybrid 44건 차단). **`smoke` 컬렉션 보존** — 회귀 시
  서버 env `CHROMA_COLLECTION=smoke`로 즉시 복구
- **server.py 개편**: `_load_aux()` 덧대기 → cards.json 직접 로드. `_llm_annotate` 삭제,
  intro/reason/filtered 은퇴 (프론트 폴백으로 무수정 동작 — index.html:730,746 실측).
  SPOT_PID NameError(/health 500) 수정. 이름 사전 = 정본명+aliases (변형 어느 것으로도 조회됨)
  - **matched 신설**: "왜 추천"을 코드가 조립 — {region, tags(질의와 겹친), bloggers}. 지어낼 수 없는 이유만
  - **폐업 안내**: 검색·브라우즈 제외, 이름 조회에는 closed 카드로 응답 (지우면 모르고 찾아감)
  - /evidence: opinion LLM 은퇴, quotes = 매칭 스니펫 코드 발췌 (스니펫도 정본으로 접음 — 프릳츠 396건/211블로거)
- **검증 9항목 통과**: 성산 카페→프릳츠 1장 / 변형 조회→정본 1장 / 애월빵공장→폐업 안내 /
  해지개 회귀 / matched 근거 / 브라우즈 union 다수결 / evidence 원문 인용 / health / 1~3초
- `FRONTEND_지침.md` **파도 7** 추가: 은퇴 필드·matched·closed·정본명 전환(⚠ spot_name 조인 은퇴)

**7/9 밤 확정 결정 (28~30)**:

28. **런타임 LLM 0**: 선별·intro·reason·opinion 전부 은퇴. "이 시스템의 실체는 검색엔진이고
    그게 강등이 아니라 정돈" (민옥). LLM의 자리 = 오프라인 파이프라인(996곳 정제·태그·요약) — 발표 서사
29. **추천 이유는 코드가**: matched(지역·태그·블로거)만 표시 — 근거가 판단을 만들지,
    판단 뒤에 근거를 지어 붙이지 않는다
30. **중복의 해법은 병합, 검수는 사람이 O/X만**: 이름 유사도 자동 병합 금지 유지.
    place_id 자동 + 알맹이 완전 일치(민옥 원칙 "같은 이름이면 같은 가게")만 코드가

**다음 후보**: ① 프론트 파도 7 반영(클로드 코드 — matched 표시·closed 배지·좌표 직접 사용)
② 일반명 노이즈 카드 정리("동쪽 일몰 맛집 카페"·"제주도 강아지동반 애견카페" 유형 — Pass 1 잔재)
③ 서귀피안 보래드 베이커스 vs 서귀피안베이커리 동일 여부 눈검수 ④ 주변 탐색(카카오 반경 검색)

## 7/9 작업 — 검색 레이어 완성 + place_id 정본화

**한 줄**: 검색 3분기(이름 조회/브라우즈/조건) + 지역 교정 + LLM 선별·이유 + /evidence 원문 근거 + 카카오 place_id 787. 상세는 `07/0709 작업 정리.md`.

- **해지개 사건 종결**: 이름 매치 레이어(사전 815) — 카페명 질의 무조건 1순위 (`name_match`)
- **지역 오염 24.6% 교정**: LLM 추측 라벨 → 주소 유도 2단 계층(버킷/세부) 880곳, 코드 필터
- **브라우즈 모드**: 빈 질의("애월 카페")는 임베딩 생략, 다수결(고유 블로거) 정렬 + total 노출
- **LLM [2.5+3]**: gpt-5-mini 선별+intro+카드별 reason. 인기 수치 입력 금지, 실패 무해. 6~9초
- **place_id 787/1,015 (78%)**: `pipeline/kakao_place.py`. 중복 122그룹 자동 검출, MISS-보류 80% 교차 검증. HOLD 57 눈 검수 큐
- **동료 시드 평가**: 지역 정합 18/20 vs 11/20 (소스 키워드 스팸) — 임베딩 채택 안 함, 라벨만 편입(caution 772·영업시간 368 → 카드 표시층)
- **/evidence**: "블로거 21명이 언급" + 원문 인용(코드 검증) + 쇼츠 댓글 반응 톤
- **FRONTEND_지침.md 신설**: 클로드 코드 작업 계약 (파도 1~4)

**7/9 확정 결정 (21~27)**:

21. **검색 3분기**: 조회(이름 사전)/브라우즈(다수결)/조건(임베딩+LLM) — 코드 라우팅, [1] LLM 의도 분류가 승계 예정
22. **지역도 식별자**: 주소에서 코드로 유도(버킷/세부 2단). LLM 추측 라벨 폐기
23. **빈 질의 정렬 = 다수결**: 원칙 8의 동점 정렬 조항의 정확한 적용
24. **place_id = 영구 키, 이름 조인 은퇴**: 네이버=텍스트·실존 / 카카오=키·좌표. 지도 SDK(액자)는 별개 — 언제든 교체 가능
25. **LLM 런타임 자리**: 선별+이유([2.5+3], 배포)와 의도 분류([1], 예정)만. 근거 없는 생성은 계속 보류, 인기 수치는 LLM 입력 금지
26. **근거는 원문 인용**: LLM 발췌를 코드가 부분문자열 검증. 반응 톤(부정·혼합) 숨김 금지 — 신뢰가 컨셉
27. **동료 데이터는 층 이식**: 구조화 키워드는 임베딩엔 스팸, 라벨층엔 자산 ("층을 옮기면 독이 약")

**다음 최우선**: merge.py 카드 정본 — 재료 완비(판정+텍스트+place_id+라벨+반응). place_id 병합으로 중복 122그룹 해소 + 층 분리 스키마 + 폐업·휴업 강등(오아시스80 사건)

## 7/8 작업 — 네이버 텍스트 보강 + MVP 관통

**한 줄**: 네이버 블로그/지역검색으로 low 카페를 구제하고, 웹→임베딩→검색→화면 관통(MVP) 완성.

- **네이버 크롤링**: 고유 카페 1,064곳 전량 (블로그 100건 + 지역검색 5건씩) → `raw/네이버 크롤링.jsonl`
- **재검색 구제**: 실존의심 85곳 이름 정리(괄호·수식어 제거) 후 재크롤 → 48곳 신호 회생 → `raw/네이버 재검색 크롤링.jsonl`
- **블로그 정제 (gpt-5-mini)**: 996곳 → `processed/네이버 정제.jsonl`
  - **richness 반전: 유튜브 기준 low 667곳 → 블로그 기준 high 947 / mid 29 / low 20**
  - closed_hint 폐업 검출 11곳 (수리코 등 — 임베딩 자동 제외)
- **판정 체계**: 상호/유효스니펫/registry 3신호 → 유지 876 / 보류 134 / 제외 49 (`review_master.csv` — 검수 뷰는 이것 하나만 유지)
- **MVP 관통**: `pipeline/embed.py`(이원 적재) + `app/server.py`(/search API) + `web/index.html`(fetch 연결, mock 자동 폴백)

**7/8 확정 결정 (기존 1~10에 추가)**:

11. **카페의 경계 = 여행자 관점**: 디저트·베이커리·소품샵·펍/음식점 겸업도 포함. 업종은 배제 기준이 아니라 태그 재료
12. **네이밍 체계**: raw = `~크롤링`, processed = `~정제` (유튜브 raw는 기존 타임스탬프 동결 관례 유지)
13. **출처 분리 유지**: 유튜브/블로그 정제본은 병합하지 않고 별도 파일 + 임베딩 메타데이터 source 라벨. 병합은 최종 카드 생성 때 1회 (summary_source 추적 가능하게)
14. **블로그 신호도 이원화**: 총량은 오염 취약(이름 흔함·옛글·도배) → "필터(카페명 포함+2024이후) 후 고유 블로거 수"가 다수결. 유튜브 channel_count와 같은 논리. 총량은 동점 정렬·카피에만
15. **태그 사전 강제는 코드가**: 프롬프트 구속은 새는 게 실측됨 → 코드 필터 + 이탈분 `tags_extra` 보존 (사전 승격 후보: 포토존 22, 핸드드립 11, 한라산뷰 10…)
16. **registry는 실존 신호로만**: 좌표는 신뢰성 문제로 미사용 (민옥 결정 7/8). 좌표 = 네이버 지역검색 mapx/mapy, 카카오 보강은 클로드 코드 담당
17. **UI 근거 이원화**: 영상 있는 카페 = MV 트랙(유튜브 썸네일), 블로그만 = 음원 트랙(그라데이션 + 📝후기 뱃지). 가짜 썸네일 금지
18. **데이터셋 주종 관계 (7/8 저녁 확정)**: **네이버 = 주 데이터셋**(RAG 검색 텍스트 전담), **유튜브 = 서브**(근거 영상 링크·썸네일·차트인 신호 전담 + hybrid 텍스트 폴백). 3자 비교 실측 근거
19. **셋리스트 기능 보류 (7/8)**: 외부 레퍼런스 이식 과정의 노이즈 + 시간·동선 데이터 근거 부재("지어내기 금지"와 충돌). 대체 방향: **주변 탐색** — 카페 좌표 기준 카카오 로컬 반경 검색(FD6 음식점/AT4 관광명소)으로 볼거리·맛집 제공. v1은 LLM 불필요
20. **정보/반응 이원화 (7/8 밤)**: 블로그 = 정보(무엇인가), 유튜브 쇼츠 댓글 = **반응**(어떻게 느끼는가) — 매체 비교우위. **반응 텍스트는 임베딩 금지**(만능 자석 양산 위험) — 자리는 카드 카피·동점 정렬·화제성 감지(댓글 velocity)·폐업 제보. 데이터 지도: 블로그=정보 / 유튜브 영상=근거 / 유튜브 댓글=반응 / 팀원 풀=뼈대. 다음 단계: 표본 30영상 댓글 수율 실증

**서빙 코퍼스 확정 (7/8 저녁, 4자 비교 실험 근거)**:
- 구성 = **hybrid 385**(팀원 병합 풀 뼈대 + 우리 자연어 요약) + **blog 851**(판정 '유지'만, 폐업 제외)
- 우리 유튜브 문서는 은퇴 — "팀원 풀이 더 정확(검증됨)" (민옥 결정). 유튜브 요약은 hybrid 텍스트 폴백으로만
- 실험 결론: 임베딩 텍스트는 자연어 문장 > 구조화 프로필 (팀원에 공유할 것)
- 남은 이슈: 만능 요약 자석(Mandi Wasan 유형) 검출 필요, seed 중복(카페 더라이트), 골든셋은 데이터 안 본 팀원에게

**영역 분담 (충돌 방지, 7/8 클로드 코드와 합의)**:
- `pipeline/` + `app/server.py` + web의 apiSearch = **코워크(SilenceBreaker) 영역**
- web의 plotCards(지도 마커) + 좌표 데이터 = **클로드 코드 영역** (카카오 좌표 데이터 보유)
- **접점 계약**: 카드 JSON의 `spot_name`이 조인 키 — 클로드 코드 좌표 테이블은 카카오 정식명칭이 아니라 정제본 spot_name 기준으로 키를 맞출 것. 장기적으로 place_id로 대체(결정 9)
- 현재 plotCards의 keywordSearch 코드는 코워크가 넣은 임시방편 — 클로드 코드가 통째로 교체 가능

**서빙 코퍼스 구성 (7/8 저녁 확정, 3자 비교 실험 근거)**:
- **hybrid 385** = 팀원 병합 풀(cafe_id/TIER 검증)이 명단 권위 + 텍스트는 우리 자연어 요약(블로그>유튜브)
- **blog 851** = 팀원 풀 밖 카페 중 판정 '유지'만 (보류/제외/폐업 차단)
- 우리 유튜브 문서는 은퇴 (팀원 풀이 더 정확 — 민옥 결정). 유튜브 요약은 hybrid 폴백 텍스트로만
- 실험 근거: 8문항 비교에서 blog 5승 / 자연어 문장 > 구조화 프로필 / 만능 요약 자석(Mandi Wasan) 발견
- 팀원이 블로그 통합 병합본(v2, 전체 ~1,000곳)을 주면 전체 교체 예정

**팀원에게 요청할 것**: ① 병합 풀 410→전체 확장, 임베딩 텍스트는 `네이버 정제.jsonl`의 summary_blog 사용 ② seed 중복 제거(카페 더라이트 2건) ③ 골든셋 25문항은 카드 데이터 안 본 사람에게 (민옥·코워크 둘 다 오염됨)

**팀원이 지금 잡을 수 있는 마디**: ① `pipeline/merge.py` 병합(place_id/이름 정규화 — 가장 큰 덩어리) ② 골든셋 25문항(⚠ 카드 데이터 안 본 사람만) ③ CD 관통(GCP Secrets)

## 프로젝트 한 줄

유튜브 영상/숏츠 기반 **제주 카페 RAG 서비스** (카페로 스코프 확정).
질문 → 실제 영상 근거 카드 → "오늘의 셋리스트(코스)" 발매. K-POP 앨범 컨셉.

## 완료된 것 (숫자로)

- **수집**: 키워드 격자 41개 + 카페 보강 18개 → raw 무손실 저장
  - `data/raw/raw_20260707_1006.json` (1,359건, 전 카테고리)
  - `data/raw/raw_20260707_cafe.json` (카페 보강 병합본 — 최신, 이걸 쓸 것)
- **카페 코퍼스**: `data/processed/cafe_corpus.json` (카페 관련 필터본)
- **Pass 1 전량 완료**: `data/processed/pass1_cafe_full.json`
  - 스팟 언급 1,504개 / 고유 이름 1,064개
  - info_richness: high 398 / mid 173 / low 933 (low는 임베딩 제외, 언급신호로만)
  - 주소 보유 393개 / 소요 40분 / gpt-5-mini (reasoning_effort=minimal 필수)
- **검수 CSV**: `data/processed/review_cafe.csv` (utf-8-sig, 엑셀용 뷰)
- **디자인 초안**: `design/jeju_pop_trail_design.html` (다크+핑크/시안, 트랙카드)
- **스모크 테스트 통과**: 임베딩(text-embedding-3-large)→Chroma→검색→셋리스트 생성 세로 관통 확인

## 확정된 설계 결정 (뒤집으려면 근거 필요)

1. **키워드 격자 수집** (채널 방식 아님 — 넓은 도메인엔 키워드가 맞음, 실측 검증됨)
2. **무손실 raw**: 설명란 0자도 저장. "정보 없는 데이터도 투표권(다수결)은 있다"
3. **이원 트랙**: A(설명란 有→슬롯 추출) / B(숏츠, 제목+태그로 카페명 특정)
4. **info_richness 3단**: low는 검색 오염 방지 위해 임베딩 제외, mention 신호로만 병합
5. **video_id 등 식별자는 LLM 우회** — 코드가 운반 (환각 방지)
6. **런타임 3단계 체인** (에이전트 아님): [1]질문 재작성 → [2]검색+필터 → [3]셋리스트 생성
   - [1]은 필수로 검증됨: "클럽이 많은 곳" → 승마클럽 사건 (표면 토큰 함정)
7. **mention_count(영상 수) + channel_count(채널 수) 이원화** — 채널 교차가 진짜 다수결
8. **점수의 자리**: 인기(차트인)는 검색이 아니라 동점 정렬+카피에만 (인기≠적합성)
9. **카카오 로컬 = Pass 2 대체**: place_id가 병합 키 + 실존검증 + 좌표 (경로1: 주소 393건)
10. **공공 카페 명부(~2,000개)** = 검증용 뼈대, 유튜브 = 추천 신호 (조인 방향: 유튜브→명부)

## 다음 할 일 (우선순위순)

1. **탐색 셀 실행** — 차트 상위 15, 병합 규모 예측, 태그 분포 (코드는 노트북에 있음)
2. **1차 병합** — (정규화 이름+region) 복합키, summary는 richness 최고 대표
   - 버킷 정규화: 종달/종달리/송당 → 구좌
3. **카카오 콘솔 스위치** ⚠ 미해결 — 403 "disabled OPEN_MAP_AND_LOCAL"
   → developers.kakao.com > 제주trip 앱 > 카카오맵 사용설정 ON
4. **place_id 재병합 + 좌표** → 최종 cards.json (스키마는 project_status.md 참조)
5. **임베딩 재구축** (low 제외, 카드 단위) → chroma_db/
6. **골든셋 25문항** ⚠ 카드 데이터 안 본 팀원이 작성해야 함 (오염 방지)
7. Streamlit 앱 / CD 관통 (GCP Secrets 등록 남음)

## 미해결 / 주의사항

- `jeju-pop-trail-scaffold\` 중첩 폴더 삭제 필요 (zip 잔재, 내용물은 루트에 이미 존재)
- 이사 후 첫 커밋 대기 중 (design/ + notebooks/ 만 신규로 잡힘 — 정상)
- config.py에 키 3종 (YT/OPENAI/KAKAO) — **절대 커밋 금지**, .gitignore가 막고 있음 확인됨
- 지역 보강 후보 (급하지 않음): 사계 26 / 대정 23 / 안덕 20 — 산방산 벨트가 얇음
- high 비율 부스트 옵션: 살란다 유형 채널 통째 수집 (선택지로만 보류)
- OpenAI 지원 키: rate limit 대비 지수 백오프 이미 코드에 있음

## 그동안 밟은 함정들 (재발 방지)

- gpt-5-mini는 추론 모델: max_completion_tokens 부족 시 **빈 응답** → 4000 + minimal
- except가 에러를 삼키면 전멸이 0개처럼 보임 → 경고 출력 필수
- 관통 표본은 코퍼스 머리가 아니라 **random.sample** (머리는 수집 이력순 편향)
- 유튜브 태그의 지역명은 스팸, 고유 장소명은 채택
- 엑셀용 CSV는 utf-8-sig (아니면 한글 깨짐)
- VS Code 실행 디렉토리 ≠ 스크립트 위치 → 경로는 ROOT 상수로 고정할 것

## 파일 지도

- 설계/현황: `project_status.md` (스키마 계약 포함) / 본 문서
- 실험실: `notebooks/데이터탐색.ipynb` (수집~탐색 셀 전부)
- 원본 백업: `C:\Users\akals\Downloads\제주도` (이사 원본, 당분간 유지)
